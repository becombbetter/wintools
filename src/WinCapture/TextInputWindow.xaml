<Window x:Class="WinCapture.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加文字" Width="420" Height="170" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="输入标注文字" FontSize="14" FontWeight="SemiBold" />
        <TextBox x:Name="ValueTextBox" Grid.Row="1" Margin="0,10" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Click="CancelButton_Click">取消</Button>
            <Button Style="{StaticResource PrimaryButton}" Click="ConfirmButton_Click" Margin="8,0,0,0">确定</Button>
        </StackPanel>
    </Grid>
</Window>
