<Window x:Class="WinCapture.RecorderSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="录屏设置" Width="520" Height="470" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="开始新的录屏" FontSize="22" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="选择录制范围和声音来源" Margin="0,5,0,20" Foreground="{StaticResource MutedBrush}" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="录制范围" FontWeight="SemiBold" Margin="0,0,0,8" />
            <ComboBox x:Name="TargetComboBox" SelectedIndex="0">
                <ComboBoxItem Tag="PrimaryScreen">主显示器</ComboBoxItem>
                <ComboBoxItem Tag="Region">选择区域</ComboBoxItem>
                <ComboBoxItem Tag="Window">选择窗口</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="声音与指针" FontWeight="SemiBold" Margin="0,0,0,6" />
                <CheckBox x:Name="SystemAudioCheckBox" Content="录制系统声音" />
                <CheckBox x:Name="MicrophoneCheckBox" Content="录制麦克风" />
                <CheckBox x:Name="CursorCheckBox" Content="显示鼠标指针" />
                <CheckBox x:Name="ClickCheckBox" Content="显示点击位置" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="24,0,0,0">
                <TextBlock Text="画质" FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBlock Text="帧率" Foreground="{StaticResource MutedBrush}" />
                <ComboBox x:Name="FrameRateComboBox" Margin="0,4,0,10">
                    <ComboBoxItem Tag="24">24 FPS</ComboBoxItem>
                    <ComboBoxItem Tag="30">30 FPS</ComboBoxItem>
                    <ComboBoxItem Tag="60">60 FPS</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="码率" Foreground="{StaticResource MutedBrush}" />
                <ComboBox x:Name="BitrateComboBox" Margin="0,4,0,0">
                    <ComboBoxItem Tag="4">4 Mbps</ComboBoxItem>
                    <ComboBoxItem Tag="8">8 Mbps</ComboBoxItem>
                    <ComboBoxItem Tag="16">16 Mbps</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </Grid>

        <Border Grid.Row="4" Margin="0,20,0,12" Background="#EAF2F1" BorderBrush="#C9DDDA" BorderThickness="1" CornerRadius="6" Padding="12">
            <TextBlock Text="录屏使用 H.264 硬件编码并保存为 MP4；窗口和区域选择会在点击开始后进行。"
                       Foreground="#365D59" TextWrapping="Wrap" />
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Click="CancelButton_Click">取消</Button>
            <Button Style="{StaticResource PrimaryButton}" Click="StartButton_Click" Margin="8,0,0,0">开始录屏</Button>
        </StackPanel>
    </Grid>
</Window>
